# tools/delete_strings_by_prefix.yaml
identity:
  name: delete_strings_by_prefix
  author: hjlarry
  label:
    en_US: Delete Strings by Prefix
  description:
    human:
      en_US: Deletes all keys beginning with the given prefix.
    llm: Deletes all keys beginning with the given prefix.
parameters:
  - name: prefix
    type: string
    required: true
    label:
      en_US: Prefix
    human_description:
      en_US: The prefix of the keys to delete.
    llm_description: The prefix of the keys to delete.
    form: llm
extra:
  python:
    source: tools/delete_strings_by_prefix.py
