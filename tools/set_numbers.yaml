# tools/set_numbers.yaml
identity:
  name: set_numbers
  author: hjlarry
  label:
    en_US: Set Numbers
  description:
    human:
      en_US: Stores multiple numeric key-value pairs.
    llm: Stores multiple numeric key-value pairs.
parameters:
  - name: list
    type: array
    required: true
    label:
      en_US: Key-Value Pairs
    form: llm
    items:
      type: object
      properties:
        key:
          type: string
          required: true
        value:
          type: number
          required: true
extra:
  python:
    source: tools/set_numbers.py
