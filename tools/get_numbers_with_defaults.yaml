# tools/get_numbers_with_defaults.yaml
identity:
  name: get_numbers_with_defaults
  author: hjlarry
  label:
    en_US: Get Numbers with Defaults
  description:
    human:
      en_US: Retrieves numbers and applies default for missing keys.
    llm: Retrieves numbers and applies default for missing keys.
parameters:
  - name: list
    type: array
    required: true
    label:
      en_US: Keys
    form: llm
    items:
      type: string
  - name: default
    type: number
    required: true
    label:
      en_US: Default Value
    form: llm
extra:
  python:
    source: tools/get_numbers_with_defaults.py
