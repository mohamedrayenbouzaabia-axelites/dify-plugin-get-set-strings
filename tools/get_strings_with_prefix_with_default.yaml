# tools/get_strings_with_prefix_with_default.yaml
identity:
  name: get_strings_with_prefix_with_default
  author: hjlarry
  label:
    en_US: Get Strings with Prefix with Default
  description:
    human:
      en_US: Retrieves multiple prefixed values; uses default if any are missing.
    llm: Retrieves multiple prefixed values; uses default if any are missing.
parameters:
  - name: prefix
    type: string
    required: true
    label:
      en_US: Prefix
    form: llm
  - name: default
    type: string
    required: true
    label:
      en_US: Default Value
    form: llm
extra:
  python:
    source: tools/get_strings_with_prefix_with_default.py
