# tools/get_numbers_with_prefix_with_defaults.yaml
identity:
  name: get_numbers_with_prefix_with_defaults
  author: hjlarry
  label:
    en_US: Get Numbers with Prefix with Defaults
  description:
    human:
      en_US: Retrieves numeric values for prefixed keys with fallback default.
    llm: Retrieves numeric values for prefixed keys with fallback default.
parameters:
  - name: prefix
    type: string
    required: true
    label:
      en_US: Prefix
    form: llm
  - name: default
    type: number
    required: true
    label:
      en_US: Default Value
    form: llm
extra:
  python:
    source: tools/get_numbers_with_prefix_with_defaults.py
