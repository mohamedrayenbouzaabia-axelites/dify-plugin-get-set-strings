# tools/set_number.yaml
identity:
  name: set_number
  author: hjlarry
  label:
    en_US: Set Number
  description:
    human:
      en_US: Stores a numeric value.
    llm: Stores a numeric value.
parameters:
  - name: key
    type: string
    required: true
    label:
      en_US: Key
    form: llm
  - name: value
    type: number
    required: true
    label:
      en_US: Value
    form: llm
extra:
  python:
    source: tools/set_number.py
