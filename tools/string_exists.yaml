# tools/string_exists.yaml
identity:
  name: string_exists
  author: hjlarry
  label:
    en_US: Check if String Exists
  description:
    human:
      en_US: Returns whether a given string key exists.
    llm: Returns whether a given string key exists.
parameters:
  - name: key
    type: string
    required: true
    label:
      en_US: Key
    human_description:
      en_US: The key to check for existence.
    llm_description: The key to check for existence.
    form: llm
extra:
  python:
    source: tools/string_exists.py
