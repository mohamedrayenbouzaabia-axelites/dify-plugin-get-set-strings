# tools/get_number_with_default.yaml
identity:
  name: get_number_with_default
  author: hjlarry
  label:
    en_US: Get Number with Default
  description:
    human:
      en_US: Returns number or default if not found.
    llm: Returns number or default if not found.
parameters:
  - name: key
    type: string
    required: true
    label:
      en_US: Key
    form: llm
  - name: default
    type: number
    required: true
    label:
      en_US: Default Value
    form: llm
extra:
  python:
    source: tools/get_number_with_default.py
