# tools/get_numbers.yaml
identity:
  name: get_numbers
  author: hjlarry
  label:
    en_US: Get Numbers
  description:
    human:
      en_US: Retrieves multiple numeric values.
    llm: Retrieves multiple numeric values.
parameters:
  - name: list
    type: array
    required: true
    label:
      en_US: Keys
    form: llm
    items:
      type: string
extra:
  python:
    source: tools/get_numbers.py

