# tools/set_string_if_not_exist.yaml
identity:
  name: set_string_if_not_exist
  author: hjlarry
  label:
    en_US: Set String if Not Exist
  description:
    human:
      en_US: Sets a new key-value pair only if the key does not already exist.
    llm: Sets a new key-value pair only if the key does not already exist.
parameters:
  - name: key
    type: string
    required: true
    label:
      en_US: Key
    form: llm
  - name: value
    type: string
    required: true
    label:
      en_US: Value
    form: llm
extra:
  python:
    source: tools/set_string_if_not_exist.py
