# tools/check_key_existence.yaml
identity:
  name: check_key_existence
  author: hjlarry
  label:
    en_US: Check Key Existence with Type
  description:
    human:
      en_US: Verifies if a key exists, and if yes, returns its type.
    llm: Verifies if a key exists, and if yes, returns its type (string or number).
parameters:
  - name: key
    type: string
    required: true
    label:
      en_US: Key
    form: llm
extra:
  python:
    source: tools/check_key_existence.py
