# tools/get_strings_with_prefix.yaml
identity:
  name: get_strings_with_prefix
  author: hjlarry
  label:
    en_US: Get Strings with Prefix
  description:
    human:
      en_US: Retrieves all key-value pairs that start with the given prefix.
    llm: Retrieves all key-value pairs that start with the given prefix.
parameters:
  - name: prefix
    type: string
    required: true
    label:
      en_US: Prefix
    human_description:
      en_US: The prefix to search for.
    llm_description: The prefix to search for.
    form: llm
extra:
  python:
    source: tools/get_strings_with_prefix.py
