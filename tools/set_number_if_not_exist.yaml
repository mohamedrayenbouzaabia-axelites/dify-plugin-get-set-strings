# tools/set_number_if_not_exist.yaml
identity:
  name: set_number_if_not_exist
  author: hjlarry
  label:
    en_US: Set Number if Not Exist
  description:
    human:
      en_US: Assigns a number only if the key is missing.
    llm: Assigns a number only if the key is missing.
parameters:
  - name: key
    type: string
    required: true
    label:
      en_US: Key
    form: llm
  - name: value
    type: number
    required: true
    label:
      en_US: Value
    form: llm
extra:
  python:
    source: tools/set_number_if_not_exist.py
