# tools/get_string_with_default.yaml
identity:
  name: get_string_with_default
  author: hjlarry
  label:
    en_US: Get String with Default
  description:
    human:
      en_US: Returns the stored value or a default if not found.
    llm: Returns the stored value or a default if not found.
parameters:
  - name: key
    type: string
    required: true
    label:
      en_US: Key
    form: llm
  - name: default
    type: string
    required: true
    label:
      en_US: Default Value
    form: llm
extra:
  python:
    source: tools/get_string_with_default.py
